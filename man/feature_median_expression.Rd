% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{feature_median_expression}
\alias{feature_median_expression}
\title{Median expression of each feature in each expressing sample}
\usage{
feature_median_expression(state, config)
}
\arguments{
\item{state}{A list with elements like that returned by \code{read_data()}:
\tabular{ll}{
  \code{expression} \cr \tab Numeric matrix with non-negative expression values. \cr
  \code{features}   \cr \tab A data.frame with feature meta-data for rows of expression. \cr
  \code{samples}    \cr \tab A data.frame with observation meta-data for columns of expression. \cr
}}

\item{config}{List with configuration values. Does not use any params, so can pass empty list.}
}
\value{
A numeric vector of length \code{nrow(state$expression)} with median
  expression over the expressing samples, and \code{NA} for any feature with
  no expressing samples.
}
\description{
Calculates the median expression of each feature in each expressing sample.
}
\details{
Median over the non-\code{NA} values of the feature. A feature measured in no
    sample has no median, and gets \code{NA}.
}
\examples{
set.seed(101)
exprs <- h0testr::sim1(n_obs=6, n_feats=12)$mat
feats <- data.frame(feature_id=rownames(exprs))
samps <- data.frame(observation_id=colnames(exprs))
state <- list(expression=exprs, features=feats, samples=samps)
config <- list()
h0testr::feature_median_expression(state, config)
}
