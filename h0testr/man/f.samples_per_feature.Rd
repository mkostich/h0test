% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{f.samples_per_feature}
\alias{f.samples_per_feature}
\title{Number of samples expressing each feature}
\usage{
f.samples_per_feature(state, config)
}
\arguments{
\item{state}{A list with elements like that returned by \code{f.read_data()}:
\tabular{ll}{
  \code{expression} \cr \tab Numeric matrix with non-negative expression values. \cr
  \code{features}   \cr \tab A data.frame with feature meta-data for rows of expression. \cr
  \code{samples}    \cr \tab A data.frame with observation meta-data for columns of expression. \cr
}}

\item{config}{List with configuration values. Does not use any params, so can pass empty list.}
}
\value{
A numeric vector of length \code{nrow(state$expression)} with non-negative sample 
  counts for each feature.
}
\description{
Calculates the number of samples expressing each feature
}
\details{
Feature considered expressed if \code{state$expression > 0}; 
  \code{NA}s count as no expression.
  See documentation for \code{h0testr::f.new_config()} 
    for more detailed description of configuration parameters.
}
\examples{
set.seed(101)
exprs <- h0testr::f.sim1(n_obs=6, n_feats=12, mcar_p=0.5)$mat
feats <- data.frame(feature_id=rownames(exprs))
samps <- data.frame(observation_id=colnames(exprs))
state <- list(expression=exprs, features=feats, samples=samps)
config <- list()
h0testr::f.samples_per_feature(state, config)
}
